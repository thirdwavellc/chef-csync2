nossl * *;
lock-timeout <%= @lock_timeout %>;
group wordpress
{
<% @hosts.each do |host| %>
        host <%= host %>;
<% end %>
        key <%= @key_path %>;
<% @synced_dirs.each do |dir| %>
        include <%= dir %>;
<% end %>
        auto younger;
}
